# Database Connection
DATABASE_URL=postgresql://username:password@localhost:5432/arabic_legal_platform

# OpenAI API for Article Explanations
AI_INTEGRATIONS_OPENAI_API_KEY=sk-your-api-key-here
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Admin User IDs (comma-separated)
ADMIN_USER_IDS=

# Server Port (optional, default: 5000)
PORT=5000

# Node Environment
NODE_ENV=development

# Google OAuth 2.0 (optional - for "Sign in with Google")
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Add redirect URI: http://localhost:3005/api/auth/google/callback (dev)
# Add redirect URI: https://your-domain.com/api/auth/google/callback (prod)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# JWT Secret (change in production!)
AUTH_JWT_SECRET=dev-change-me

# Admin emails (comma-separated, for role assignment)
ADMIN_EMAILS=abooood01@gmail.com
